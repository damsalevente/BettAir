#include "cosensor.h"
#include "lightsensor.h"
#include "rgb.h"
#include "tempsensor.h"

int setup()
{
  /* init sensors, check if they can be read */
  /* init rgb light */
  /* init wifi module */
  /* if any error, light red light for indication */
  /* leds for detecting erros */
}
int run()
{
  
}
int main()
{
    /* read sensor 1 */
    /* read sensor 2 */
    /* read sensor 3 */
    /* calculate error status */
    /* send it to server */
    /* update old values */
    delay(1000);
    return 0;
}
